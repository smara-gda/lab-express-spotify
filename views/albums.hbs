

<div class="results">
   {{#each albums}}
   <div class="result">
    <div class="">
     <img class="img-style"src='{{this.images.[1].url}}' alt="{{name}}">
    </div>
    <div class="button-wrapper">
     <p class="name-style">{{name}}</p>   
     <button class="button-style"> <a href="/tracks/{{this.id}}">View tracks</a> </button>
    </div>
   </div>
   {{/each}}
</div>